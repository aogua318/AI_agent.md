# 使用规范

**用 SRP 约束会话的目标数量，用文档和 skill 承担跨目标的共享知识**

| 值得固化                             | 不值得固化                                 |
| ------------------------------------ | ------------------------------------------ |
| 事实（表结构、字段含义、枚举值）     | 过程（“我试了 A 不行又试了 B”）            |
| 约定（命名规范、测试命令、部署流程） | 临时决策（“这次先这么办”）                 |
| 失败模式（“order 表全表扫描会超时”） | 一次性结论（只在本次推导中有用的中间结果） |



值的固化的东西
决策    为什么用了python
已知事实   API的返回结构
踩坑     报错

会话内容保存
结论
原因
约束
结果

项目文档分成两类
项目事实文档


## 项目文档结构

docs/
├── architecture.md
├── design/
├── decisions/
├── troubleshooting/
├── api/
└── development-notes/

格式



# 首先把上下文分成 5 种东西

这是整个体系最核心的部分。

很多 AI 使用问题，本质上都是把不同类型的信息混在了一起。

> ## 1. Instruction：规则
>
> 回答的是：
>
> > **AI 应该怎么工作？**
>
> 例如：
>
> ```
> 变量类型明确
> 代码遵循软件工程规范
> 注释详细
> 优先修改现有代码而不是重写
> 不要擅自改变架构
> ```
>
> 放：
>
> ```
> 用户级规范
> AGENTS.md
> ```
>
> 这类信息通常比较稳定。
>
> ------
>
> ## 2. Knowledge：知识
>
> 回答：
>
> > **项目已经知道什么？**
>
> 例如：
>
> ```
> 项目使用 Python + PySide6
> 
> GameState 负责游戏状态
> 
> Board 负责棋盘数据
> 
> UI 不直接修改游戏状态
> 
> 炸弹生成采用……
> ```
>
> 放：
>
> ```
> docs/
> ```
>
> 这是**项目长期记忆**。
>
> ------
>
> ## 3. State：状态
>
> 回答：
>
> > **项目现在是什么状态？**
>
> 例如：
>
> ```
> 当前正在实现设置系统
> 
> 扫雷核心逻辑已完成
> 
> UI 已完成 70%
> 
> 当前阻塞问题：
> 窗口缩放后棋盘尺寸计算错误
> ```
>
> 这个和知识非常不同。
>
> “采用 PySide6”是知识。
>
> “设置系统还没做完”是状态。
>
> 状态会快速变化。
>
> 所以最好单独管理。
>
> 例如：
>
> ```
> docs/ai/current-state.md
> ```
>
> 或者：
>
> ```
> STATUS.md
> ```
>
> ------
>
> ## 4. Experiment：探索过程
>
> 回答：
>
> > **我们尝试过什么？**
>
> 例如：
>
> ```
> 尝试方案 A
> → 性能不好
> 
> 尝试方案 B
> → 能运行，但是架构不合理
> 
> 尝试方案 C
> → 已验证可行
> ```
>
> 这类信息不一定全部进入长期文档。
>
> 因为：
>
> > **探索过程 ≠ 最终知识。**
>
> 可以放：
>
> ```
> docs/experiments/
> ```
>
> 需要的时候再查。
>
> ------
>
> ## 5. Task Context：当前任务
>
> 回答：
>
> > **我们这一次到底要干什么？**
>
> 例如：
>
> ```
> 目标：
> 实现扫雷窗口自适应缩放
> 
> 约束：
> UI 保持 XP 风格
> 
> 不要：
> 修改核心游戏规则
> 
> 验收：
> 800×600
> 1920×1080
> 4K
> 都能正常显示
> ```
>
> 这个主要存在于**当前会话**。

# `docs/ai/current-state.md` 非常重要

我尤其建议你增加这个东西。

因为：

```
AGENTS.md
```

解决的是：

> **应该怎么开发**

而：

```
current-state.md
```

解决的是：

> **现在开发到哪里了**

例如：

```
# 当前项目状态

## 已完成

- 棋盘数据结构
- 雷区生成
- 左键点击
- 右键标记
- 数字计算

## 当前开发

设置系统

## 下一步

实现：
- 雷数设置
- 棋盘尺寸设置
- 难度保存

## 当前已知问题

窗口缩放后棋盘重绘存在问题。

## 最近架构变更

2026-09-18：
将 GameState 与 BoardView 分离。
```

# 然后是“上下文加载优先级”

这个非常关键。

**不要每次把所有项目文档都塞给 AI。**

应该采用分层加载。

我建议：

```
Level 0
全局规则
   ↓
Level 1
项目规则 AGENTS.md
   ↓
Level 2
项目当前状态
   ↓
Level 3
与当前任务直接相关的知识
   ↓
Level 4
相关源码 / 配置 / 日志
   ↓
Level 5
当前聊天历史
```

也就是说：

> **距离当前任务越近的信息，越应该优先进入上下文。**


固定循环
探索→讨论→试错→形成结论→沉淀文档→压缩上下文→开启新会话→读取必要文档→继续开发

