# 项目名称

> 项目根目录：`D:\game\CEO`

---

## 1.环境介绍

### 1.1  目的

本项目主要是对`D:\game\CEO`与`D:\gameapp\Steam\steamapps\common\創業王`这两个单机游戏项目进行逆向学习的工作，包括但不限于、逆向代码逻辑、功能分析、内容复刻、修改器制作与分析，功能扩展等。

### 1.2  环境与工作区

例如：其中`D:\game\CEO`下的版本是v1.02,

​	`D:\gameapp\Steam\steamapps\common\創業王`下的版本是v1.10，当前以分析v1.10的内容为主。

### 1.3 项目架构规范

- 工作区为根目录，在根目录下创建ai_output目录，所有新产出的文件都在这个目录下。
- 根据实际情况在ai_output目录下创建对应的目录以及子目录，给产出文件分类
- 结束时判断，当前产出的所有文件是否需要重新全部整理归纳，如果需要，则允许对文件位置在ai_output目录下进行大的变动。

## 2. 前置任务

### 2.1 前置skill

这类任务优先使用 reverse-skill skill来进行分析，如果用户没有装，则推荐用户先装reverse-skill，然后根据reverse-skill的内容执行

### 2.2 前置工作

执行任务之前，先查看当前工作区有哪些值得阅读的文档。

### 2.3  工作规范

- 所有分析的发现的思路、遇到的错误与生成的成果结论都要及时写入文档，供用户或者其他环境的AI来迅速了解当前的情况与分析的过程。
- **涉及用户资产或破坏性操作前先确认**
- **涉及到下载安装的需求**：优先让用户手动处理。
- **涉及到操作第三方应用**：如果有对应的MCP并且需要安装MCP工具则提醒用户安装MCP，优先通过MCP操作，没有MCP，再用用户操作，最后才是AI来想办法处理
- **如实报告**：测试/注入失败、现象不符预期时必须明确说出，不得隐瞒或用"应该已经生效"之类措辞掩盖；**不得声称执行了实际没执行的操作**（全局规范要求，本项目尤甚——内存修改的假阳性代价很高）。
- **给建议要说明理由**；存在多个可行方案，列出各自优缺点与风险，**让用户决定**，不要替用户拍板；对可以安全判断的小问题直接处理，不因无关细节频繁询问。
- **需要用户配合时**：给出明确、可执行的步骤（点哪个按钮、看哪个数字、预期现象是什么、没反应时怎么办），一次只要求一步，并在收到反馈后再继续。

---

## 3. 过程文档化（强制）

**每个任务（一个功能点 / 一次调试会话 / 一次工具链变更 / 一次核验）完成后，必须写档再收尾**，不许只做不说。

### 3.1 文档质量要求

1. **可复现**：
2. **证据分级**：
3. **坑按"现象→根因→解法"写**。
4. **方法比结论更重要**：

3.2 文档结构要求
文档按需分类，比如踩坑文档专门用来写犯过的错，踩过的坑。进度文档:每完成一个大任务则记录产出了什么东西，过程中哪些值得记录的收获。以此类推。

---

## 4. 工作方法（新功能的标准流程）

1. **规格**：
2. **静态定位（首选，先做）**：
3. **活进程验证**：
4. **单点实测**：先只改一个目标、验证游戏侧表现（UI 数字/贴图/存档持久性），确认后再批量。
5. **批量与封装**：涉及到CE，优先"纯 Lua + 符号寻址"（免疫重定位）
6. **收尾写档**

### 4.1 交互/验证注意事项

- 需要用户配合的操作**要给出明确、可执行的指令**
- 工作过程中，如果有对该项目工作更好用的工具，则可以向用户推荐

### 4.2 沟通与验证规范

## 6. 接续工作入口（会话重启后按需读）

| 目的 | 先读 | 再读 |
|---|---|---|
| **了解项目全貌 / 决定下一步做什么** | 文档 | 需要细节时再按下面几行深入 |

